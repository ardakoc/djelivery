import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import { faAt } from '@fortawesome/free-solid-svg-icons'

export default function Email() {
  return <FontAwesomeIcon icon={faAt} style={{color: "#12b886",}} />
}